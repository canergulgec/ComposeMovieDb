ext {
    compileSdk = 30
    minSdk = 23
    targetSdk = 30

    // AndroidX
    appcompat = "1.4.2"
    core_ktx = "1.8.0"
    dataStore = "1.0.0"
    lifecycle_runtime = "2.6.0-alpha01"
    lifecycle_viewmodel = "2.6.0-alpha01"
    splashScreen = "1.0.0"

    // Google
    materialDesign = "1.6.1"

    // Compose
    compose_ui = "1.2.0"
    compose_tooling = "1.2.0"
    compose_util = "1.2.0"
    compose_material = "1.2.0"
    compose_navigation = "2.5.1"
    compose_paging = "1.0.0-alpha15"

    // Accompanist
    flowlayout = "0.25.0"
    ui_controller = "0.25.0"
    pager = "0.25.0"

    // Ktor
    ktor = "1.6.7"

    // Hilt
    hilt_navigation = "1.0.0"

    // App Debugger
    flipper = "0.80.0"
    soloader = "0.10.1"

    // Image Loading
    coil = "2.0.0"

    // Testing
    espresso = "3.3.0"
    junit = "4.13.2"

}
