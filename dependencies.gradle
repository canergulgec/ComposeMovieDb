ext {
    compileSdk = 32
    minSdk = 23
    targetSdk = 32

    // AndroidX
    appcompat = "1.5.1"
    core_ktx = "1.8.0"
    dataStore = "1.0.0"
    lifecycle_runtime = "2.6.0-alpha01"
    lifecycle_viewmodel = "2.6.0-alpha01"
    splashScreen = "1.0.0"

    // Google
    materialDesign = "1.6.1"

    // Compose
    compose_ui = "1.2.1"
    compose_tooling = "1.2.1"
    compose_util = "1.2.0"
    compose_material = "1.2.1"
    compose_navigation = "2.5.2"
    compose_paging = "1.0.0-alpha15"

    // Accompanist
    flowlayout = "0.25.1"
    ui_controller = "0.25.0"
    pager = "0.25.1"

    // Ktor
    ktor = "1.6.7"

    // Hilt
    hilt_navigation = "1.0.0"

    // Timber
    timber = "5.0.1"

    // Lottie
    lottie = "4.2.2"

    // Firebase BOM
    firebase_version = "30.3.2"

    // Image Loading
    coil = "2.0.0"

    // Testing
    espresso = "3.3.0"
    junit = "4.13.2"

}
