ext {
    compileSdk = 30
    minSdk = 23
    targetSdk = 30

    // AndroidX
    appcompat = "1.4.1"
    core_ktx = "1.7.0"
    dataStore = "1.0.0"
    lifecycle_runtime = "2.4.0"
    lifecycle_viewmodel = "2.4.0"
    splashScreen = "1.0.0-beta01"

    // Google
    materialDesign = "1.5.0"

    // Compose
    constraintlayout = "1.0.0-rc02"
    navigation = "2.4.0-alpha10"
    paging = "1.0.0-alpha14"
    activity_compose = "1.4.0"

    // Accompanist
    flowlayout = "0.20.0"
    ui_controller = "0.20.0"
    pager = "0.20.0"

    // Ktor
    ktor = "1.6.7"

    // Hilt
    hilt_navigation = "1.0.0-rc01"

    // App Debugger
    flipper = "0.80.0"
    soloader = "0.10.1"

    // Image Loading
    coil = "1.4.0"
    landscapist_glide = "1.4.5"

    // Testing
    espresso = "3.3.0"
    junit = "4.13.2"

}
