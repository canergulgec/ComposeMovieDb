ext {
    compileSdk = 30
    minSdk = 23
    targetSdk = 30

    // AndroidX
    appcompat = "1.4.2"
    core_ktx = "1.8.0"
    dataStore = "1.0.0"
    lifecycle_runtime = "2.6.0-alpha01"
    lifecycle_viewmodel = "2.6.0-alpha01"
    splashScreen = "1.0.0-rc01"

    // Google
    materialDesign = "1.6.1"

    // Compose
    compose_constraintlayout = "1.0.1"
    compose_navigation = "2.5.0"
    compose_activity = "1.4.0"
    compose_material = "1.2.0-rc02"
    compose_paging = "1.0.0-alpha15"

    // Accompanist
    flowlayout = "0.24.12-rc"
    ui_controller = "0.24.12-rc"
    pager = "0.24.12-rc"

    // Ktor
    ktor = "1.6.7"

    // Hilt
    hilt_navigation = "1.0.0"

    // App Debugger
    flipper = "0.80.0"
    soloader = "0.10.1"

    // Image Loading
    coil = "2.0.0"

    // Testing
    espresso = "3.3.0"
    junit = "4.13.2"

}
